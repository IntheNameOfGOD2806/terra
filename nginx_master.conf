server {
    listen 80;
    server_name rancher.tranthanhdat.org;

    location / {
        # Đẩy traffic về con app nội bộ
        proxy_pass https://127.0.0.1:444;
        proxy_http_version 1.1;
        
        # Bắt buộc phải có để giữ kết nối WebSockets (nếu app có dùng)
        # proxy_set_header Upgrade $http_upgrade; 
        # proxy_set_header Connection 'upgrade';
        
        # Chuyển thông tin IP thật của client cho con app backend biết
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Bypass cache của Nginx (nếu muốn app tự lo cache)
        # proxy_cache_bypass $http_upgrade;
    }
}